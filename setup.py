from setuptools import setup, find_packages

setup(
    name="kira_activation",
    version="1.0.0",
    author="Gbane Assane",
    description="Asymmetric Psi-Transcendence Complex Activation",
    packages=find_packages(),
    install_requires=["torch>=2.0.0", "numpy>=1.20.0"],
)
